change: core-scanner
title: "Fase 1 — Core Scanner"
created: 2026-07-06
artifact_store: openspec
scope: |
  Fase 1 del roadmap Nitidoc: camara en vivo + deteccion de documento +
  captura + correccion de perspectiva + editor manual de esquinas.
  Criterio de aceptacion: escaneo de 1 pagina con warp correcto en movil y desktop.

phases:
  explore:  { status: done, artifact: exploration.md }
  propose:  { status: done, artifact: proposal.md }
  spec:     { status: done, artifact: specs/ }
  design:   { status: done, artifact: design.md }
  tasks:    { status: done, artifact: tasks.md }
  apply:
    status: in_progress
    artifact: tasks.md
    branch: feat/scanner-scaffold
    slices:
      A-scaffold: { status: done, group: 1, verified: true, bundle_gzip_kb: 50.35 }
      B-worker:   { status: done, group: 2, also: [7.1], branch: feat/scanner-worker, verified: true, review: passed, fixes: [C1, C2, H1, L2], tests: 25 }
      C-camera:   { status: done, group: 3, branch: feat/scanner-camera, verified: true, review: passed, fixes: [C1, C2, H1, M1, M3, H2], tests: 40 }
      D-detection:{ status: done, group: 4, branch: feat/scanner-detection, verified: true, review: passed, fixes: [C1, C2, H1, H2, M2, M4, M1], tests: 70 }
      E-editor:   { status: done, group: 5, branch: feat/scanner-editor, verified: true, review: passed, fixes: [C1, C2, H1, H2, H3, M3, M1, L2], tests: 105 }
      F-edgecases:{ status: done, groups: [6, 7.2], branch: feat/scanner-edgecases, verified: partial, review: passed, fixes: [HIGH1, HIGH2, M1, M2, LOW2], tests: 130, e2e_fixture: degraded, open_risk: opencv-worker-init-unverified }
  opencv_worker_init:
    status: RESOLVED
    branch: fix/opencv-classic-worker
    fix: worker clasico (type:classic) + importScripts('/opencv/opencv.js') (asset copiado a public/opencv por scripts/copy-opencv.mjs)
    verified_browser: "produccion build: OpenCV listo en 540ms (msgs has-then-waiting->ready-then); sin banner degradado"
    caveat_dev: "vite dev no bundlea el worker -> OpenCV NO corre en `npm run dev` (falla rapido + degrada). Root cause diagnosticado. DECIDIDO: aceptar dev degradado + documentar; testear OpenCV con build+preview. Fix parcial commiteado (URL absoluta, e6ac1a6). README.md creado."
  remaining:
    - QA dispositivo: precision deteccion doc real, calibracion R1/R5, orderCorners fotos rotadas, feel lupa/handles
    - sdd-verify de todo el change + merge/PRs cuando haya remoto
  verify:   { status: pending, artifact: verify-report.md }
  archive:  { status: pending }

execution_mode: auto
delivery_strategy: pending   # se define en fase tasks/apply
